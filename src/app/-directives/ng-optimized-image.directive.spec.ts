import { NgOptimizedImageDirective } from './ng-optimized-image.directive';

describe('NgOptimizedImageDirective', () => {
  it('should create an instance', () => {
    const directive = new NgOptimizedImageDirective();
    expect(directive).toBeTruthy();
  });
});
